const beginner = Array(100, 200, 300, 400, 500);

function start() {
    let HashMap = Java.type('java.util.HashMap');
    let options = new HashMap();
    const jobId = cm.getJob();
    switch (jobId) {
        case 0:

    }
}